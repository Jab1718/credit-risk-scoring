Ứng dụng tính credit scoring bằng model lightGBM và tích hợp SHAP (SHapley Additive exPlanations) để giải thích lý do mô hình chấp thuận hoặc từ chối một hồ sơ vay.
Run app.py để có thể chạy trên local
Tập file train và test được lấy trên kaggleng tính credit scoring bằng model lightGBM và tích hợp SHAP (SHapley Additive exPlanations) để giải thích lý do mô hình chấp thuận hoặc từ chối một hồ sơ vay.
Run app.py để có thể chạy trên local
Tập file train và test được lấy trên kaggle
